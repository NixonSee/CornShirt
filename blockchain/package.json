{
  "type": "module",
  "private": true,
  "devDependencies": {
    "@openzeppelin/contracts": "^5.6.1",
    "dotenv": "^17.4.2",
    "hardhat": "^3.9.0"
  },
  "scripts": {
    "compile": "hardhat compile",
    "node": "hardhat node",
    "check:sepolia": "node scripts/deploy.mjs --check",
    "deploy": "hardhat run scripts/deploy.mjs --network sepolia",
    "deploy:marketplace": "node scripts/deploy-marketplace.mjs",
    "test:ticket": "hardhat run test/CornShirtTicket.test.ts --network localhost",
    "test:marketplace": "hardhat run test/CornShirtMarketplace.test.ts --network localhost"
  }
}
